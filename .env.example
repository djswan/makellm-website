# Claude API token used by /api/generate to write the llms.txt.
# The serverless function reads CLAUDE_API_TOKEN (falls back to ANTHROPIC_API_KEY).
# Set the real value in Vercel project settings → Environment Variables.
CLAUDE_API_TOKEN=
